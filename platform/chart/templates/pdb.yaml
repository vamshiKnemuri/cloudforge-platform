apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "cloudforge.name" . }}
  labels:
    {{- include "cloudforge.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "cloudforge.selectorLabels" . | nindent 6 }}

